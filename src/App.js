import React from "react";

const App = props => {
  return (
    <div className="container">
      <button type="button" className="btn btn-primary">
        App
      </button>
    </div>
  );
};

export default App;
